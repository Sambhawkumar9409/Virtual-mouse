# Virtual-mouse
This project is a virtual mouse system built using Python, OpenCV, MediaPipe, and PyAutoGUI. It allows the user to control the mouse pointer using hand movements captured through a webcam. The cursor moves according to the position of the index finger, and a pinch gesture between the thumb and index finger is used to perform mouse click actions.
